# Apify API token — get yours at https://console.apify.com/account#/integrations
APIFY_TOKEN=apify_api_xxxxxxxxxxxxxxxxxxxx

# Your wallet address to receive Tempo/PathUSD payments
# Generate one at https://app.tempo.finance or use any EVM-compatible wallet
RECIPIENT_ADDRESS=0xYourWalletAddressHere

# Server signing key used by mppx for on-chain payment verification
# Auto-generated on first run if not set — copy the logged value to persist it
SERVER_PRIVATE_KEY=0xYourServerPrivateKeyHere

# Internal mppx session secret — any random string works
# Auto-generated on first run if not set — copy the logged value to persist it
MPP_SECRET_KEY=your-random-secret-here

# Server port (optional, defaults to 3000)
PORT=3000
